//--------------------------------------------------------------------------------

use uo;
use os;

//--------------------------------------------------------------------------------

program ShowRecv(Parms)

  var who    := Parms[1];
  var Packet := Parms[2];

  print("RECV ["+hex(Packet.GetInt8(0))+","+Packet.GetSize()+"]: "+Packet);

endprogram

//--------------------------------------------------------------------------------
