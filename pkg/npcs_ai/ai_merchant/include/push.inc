//-----------------------------------------------------------------------------function AISys_Merchant_Push(Event)  if(_AI_Merchant.clockPush + 5 < ReadGameClock())    _AI_Merchant.clockPush := ReadGameClock();    if(IsSetFlag(Self(), NPCF_NO_WANDER))      AISys_Speech_Say(Event.source, AI_SPID_DONT_BARGE);    else        if(MoveIfBlocking(Self(), Event.Source))        AISys_Speech_Say(Event.source, AI_SPID_PUSHED);      else        AISys_Speech_Say(Event.source, AI_SPID_DONT_BARGE);      endif    endif  endifendfunction//-----------------------------------------------------------------------------