//--------------------------------------------------------------------------------

use uo;
use os;

//--------------------------------------------------------------------------------

program OnRemoveBoard(who,board,item)

  start_script("piece_chk",{Item,board});

  if(who.isa(POLCLASS_MOBILE))
    PlaySoundEffect(who, 0x3B1);
  endif

endprogram

//--------------------------------------------------------------------------------
